import React from "react";
import "./Timer.css";

export default function Timer() {
	return (
		<div>
			<h2>25:00</h2>
			<button>Start</button>
			<button>Pause</button>
			<button>Reset</button>
		</div>
	);
}
